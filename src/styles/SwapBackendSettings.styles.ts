import { StyleSheet } from 'react-native';

export const createSwapBackendSettingsStyles = () =>
  StyleSheet.create({
    section: { marginTop: 28 },
    reset: { paddingVertical: 12, alignItems: 'center' },
  });
