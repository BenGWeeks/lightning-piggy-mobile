import { StyleSheet } from 'react-native';

export const bgStyle = StyleSheet.create({
  full: {
    position: 'absolute',
    width: 350,
    height: 350,
    right: -60,
    top: -50,
    opacity: 0.75,
  },
});
