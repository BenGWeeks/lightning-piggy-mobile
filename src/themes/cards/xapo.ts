import { StyleSheet } from 'react-native';

export const bgStyle = StyleSheet.create({
  full: {
    position: 'absolute',
    width: 100,
    height: 60,
    right: 20,
    bottom: 20,
    opacity: 0.95,
  },
});
