import { StyleSheet } from 'react-native';

export const bgStyle = StyleSheet.create({
  full: {
    position: 'absolute',
    width: 300,
    height: 300,
    right: -60,
    top: -40,
    opacity: 0.5,
  },
});
