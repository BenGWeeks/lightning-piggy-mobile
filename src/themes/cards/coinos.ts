import { StyleSheet } from 'react-native';

export const bgStyle = StyleSheet.create({
  full: {
    position: 'absolute',
    width: 60,
    height: 60,
    right: 12,
    bottom: 12,
    opacity: 0.85,
  },
});
